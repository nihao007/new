# 3066H 多关节电机控制工程——代码整体逻辑与架构说明

## 1. 工程定位

本工程是运行在海思 Hi3066H MCU 上的裸机电机控制程序，一份固件同时包含：

- 1 路步进电机控制（协议 ID `0x01`）；
- 3 路 BLDC 无刷电机控制（协议 ID `0x02`、`0x03`、`0x04`）；
- UART1 串口指令接收、角度查询和急停；
- ADC 电位器角度反馈；
- BLDC 的 FG 测速、位置环、速度 PI 环、重力补偿和堵转处理；
- 通过板地址让多块板共用一套代码和一条串口总线。

当前 `main()` 中把 `g_boardId` 硬编码为 `0x02`，因此当前固件按“步进电机板”运行，只认领协议 ID `0x01`。

---

## 2. 项目总体框架

本项目的主线是：上位机通过串口下发电机指令，`main.c` 负责解析和分发，电机模块计算应该如何运动，最后由 Hi3066H 外设和 HAL 驱动操作真实硬件。

```text
┌───────────────────────────────────────┐
│             电脑 / 上位机             │
│  设置目标角度、查询角度、发送急停命令  │
└──────────────────├────────────────────┘
                  │ UART1 五字节协议
                  ▼
┌───────────────────────────────────────┐
│          通信层：user/uart/           │
│  UART 中断接收、环形缓冲、多板 TX 复用  │
└──────────────────├────────────────────┘
                  ▼
┌───────────────────────────────────────┐
│           调度层：user/main.c         │
│ 协议解析、校验、板地址分发、周期任务调度 │
└──────────────────├────────────────────┘
                  │
          ┌─────────└─────────┐
          ▼                   ▼
┌──────────────────┐   ┌──────────────────┐
│ 步进电机控制模块 │   │  BLDC 控制模块  │
│ 角度→步数→APT脉冲 │   │ 位置环→速度环→PWM │
└─────────├────────┘   └─────────├────────┘
          └──────────┬──────────┘
                    ▼
┌───────────────────────────────────────┐
│       公共算法层：ADC + PID          │
│       反馈采样、角度换算、PI/PID       │
└──────────────────├────────────────────┘
                  ▼
┌───────────────────────────────────────┐
│   外设初始化和 HAL：system_init.c       │
│   GPIO / UART / ADC / GPT / APT / Timer    │
└──────────────────├────────────────────┘
                  ▼
┌───────────────────────────────────────┐
│    SDK 层：chip/ + drivers/ + middleware/ │
└──────────────────├────────────────────┘
                  ▼
        MCU / 电机驱动器 / 电机 / 传感器
```

可以把各层的问题简化为：

- 通信层：电脑发来了什么？
- 调度层：这条命令应该交给哪块板、哪台电机？
- 电机控制层：电机具体应该怎样动？
- 算法层：当前状态是什么，输出应该是多少？
- 驱动层：如何把结果变成 ADC 采样、GPIO 电平或 PWM 波形？

---

## 3. 项目目录框架

```text
3066H_hapimm/
│
├─ user/                       用户业务代码，日常开发的主要区域
│  ├─ main.c                   程序入口、协议解析、板地址分发、任务调度
│  ├─ motors/
│  │  ├─ motor_stepper.c      步进电机方向、使能、步数和 APT 脉冲
│  │  └─ motor_bldc.c         BLDC 位置、速度、FG 反馈和 PWM 输出
│  ├─ uart/
│  │  ├─ uart_ring.c           UART1 中断接收和环形缓冲
│  │  └─ uart_bus.c            多板地址引脚和 TX 复用
│  ├─ adc/adc_user.c             ADC 软触发采样和角度换算辅助
│  ├─ pid/pid.c                  通用 PID/PI 计算
│  └─ generatecode/
│     ├─ system_init.c          时钟、引脚、外设和中断初始化
│     ├─ main.h                 全局外设句柄和回调声明
│     └─ feature.h              SDK 功能宏和调试串口配置
│
├─ drivers/                    Hi3066H ADC/GPIO/UART/GPT/APT/Timer 等 HAL 驱动
├─ chip/                       3066H 启动代码、芯片初始化和链接配置
├─ middleware/                 SDK 中间件、控制库和预编译库
├─ build/                      GN/Ninja 构建脚本和工具链配置
├─ out/                        编译中间文件和最终固件
│  └─ bin/target.elf|bin|hex   调试、烧录产物
├─ board.json                  时钟、引脚和外设的工程配置
└─ 3066H_hapimm.hiproj         HiSpark Studio 工程文件
```

阅读业务代码时先聚焦 `user/`。`drivers/`、`chip/`和 `middleware/` 是 SDK 基础设施，只有调查 HAL 实现或芯片底层问题时才需要深入。

---

## 4. 项目的调度模型

本项目没有操作系统和线程，是典型的裸机架构：

```text
一次性初始化 + 永久主循环 + 硬件中断 + 任务标志位
```

### 4.1 一次性初始化

MCU 上电后进入 `main()`，通过 `SystemInit()` 配置 GPIO、UART、ADC、GPT、APT、Timer 和 IRQ，然后初始化串口缓冲、总线地址等业务状态。

### 4.2 硬件中断负责“快速响应”

| 中断 | 工作 |
|---|---|
| UART1 RX | 收到一个字节后存入环形缓冲 |
| TIMER1 | 按 5 ms 时基设置速度环、位置环和遥测标志 |
| FG GPIO | 记录 BLDC FG 脉冲到达时间 |
| APT0 | 每个步进脉冲计数，到达目标后停止 APT |

中断中尽量不做串口帧解析、ADC 采样、PID 计算等耗时工作。

### 4.3 主循环负责“真正处理”

```c
while (1) {
    if (g_spdFlag) {
        执行BLDC速度环();
    }

    if (g_posFlag) {
        执行BLDC位置环();
    }

    UartPump();

    if (g_telFlag) {
        输出诊断信息();
    }
}
```

这个模型的核心分工是：

```text
中断：数据来了先收下，时间到了先置标志
主循环：解析指令、读 ADC、计算 PID、更新电机输出
硬件外设：在 CPU 不逐个翻转 GPIO 的情况下持续生成 PWM/STEP
```

对当前 `g_boardId=0x02` 的步进板，BLDC 标志虽然仍会产生，但由于本板不认领 ID02/03/04，BLDC 处理会跳过。当前主要有效路径是 UART1 接收、`UartPump()` 解析、`Stepper_RunToAngle()` 计算以及 APT0 脉冲输出。

---

## 5. 软件分层

```text
电脑/上位机
    │  UART1五字节协议
    ▼
main.c：协议解析、板地址分发、任务调度
    │
    ├── motor_stepper.c：步进角度转步数、APT 脉冲启停
    ├── motor_bldc.c：BLDC 位置环、速度环、FG 测速、PWM 输出
    ├── pid.c：通用 PID/PI 运算
    ├── adc_user.c：ADC 触发、采样平均、角度换算辅助
    ├── uart_ring.c：UART1 中断接收和环形缓冲
    └── uart_bus.c：板地址引脚和 UART TX 复用
          │
          ▼
system_init.c：时钟、GPIO、UART、ADC、GPT、APT、Timer 初始化
          │
          ▼
chip/ + drivers/：Hi3066H 芯片和 HAL 驱动
```

### 主要文件职责

| 文件 | 职责 |
|---|---|
| `user/main.c` | 系统入口、板/电机映射、串口协议、BLDC 周期调度 |
| `user/generatecode/system_init.c` | 外设句柄、引脚复用、中断和时钟初始化 |
| `user/motors/motor_stepper.c` | 步进电机运动、停止、角度换步数 |
| `user/motors/motor_bldc.c` | BLDC 驱动、FG 反馈、速度 PI、位置控制 |
| `user/pid/pid.c` | PID 计算、积分和输出限幅 |
| `user/adc/adc_user.c` | ADC SOC0 软触发，连续 4 次采样后取平均 |
| `user/uart/uart_ring.c` | UART1 逐字节中断接收、256 字节环形缓冲 |
| `user/uart/uart_bus.c` | GPIO2_7/GPIO3_0 地址采样，UART1 TX/GPIO 复用 |

---

## 6. 板地址与电机 ID

`g_boardId` 是“板地址/板类型”，串口帧中的 ID 是“电机编号”，两者不是同一个概念。

| `g_boardId` | 当前板职责 | 认领的协议 ID | 电机类型 |
|---:|---|---|---|
| `0x02` | 步进板 | `0x01` | 步进电机 |
| `0x01` | 双无刷板 | `0x02`、`0x03` | BLDC |
| `0x00` | 单无刷板 | `0x04` | BLDC |

分发由 `Bus_IsMyMotor()` 完成。所有板可以收到同一条总线上的帧，但只有认领该电机 ID 的板执行，其他板丢弃。

原设计通过跳线读取板地址：

```c
g_boardId = UartBus_GetAddr();
```

当前调试代码为：

```c
g_boardId = 0x02;
```

所以当前只执行 ID `0x01` 的步进电机指令，BLDC 的周期代码仍在主循环中，但由于电机 ID 不匹配而被跳过。

---

## 7. `main()` 启动流程

```text
main()
  │
  ├─ SystemInit()
  │    ├─ GPIO / IO 复用
  │    ├─ GPT0/GPT1（BLDC PWM）
  │    ├─ UART1（115200, 8N1）
  │    ├─ TIMER1（5 ms 控制时基）
  │    ├─ TIMER2（FG 测速时基）
  │    ├─ APT0（步进 STEP 脉冲）
  │    └─ ADC0/1/2/3
  │
  ├─ DBG_UartPrintInit(115200)
  ├─ 启动 GPT0/GPT1
  ├─ UART_Ring_Init()：开始 UART1 中断收字节
  ├─ UartBus_Init()：初始化地址脚和 TX 复用
  ├─ 确定 g_boardId
  ├─ 对本板 BLDC 执行上电解锁
  └─ 进入 while (1)
```

当前步进板上，BLDC 解锁循环找不到本板 BLDC，因此不会实际操作 BLDC 引脚。

---

## 8. 主循环调度架构

```text
while (1)
  │
  ├─ g_spdFlag == 1 ?
  │    └─ 执行本板 BLDC 5 ms 速度环
  │
  ├─ g_posFlag == 1 ?
  │    └─ 执行本板 BLDC 20 ms 位置环/堵转处理
  │
  ├─ UartPump()
  │    └─ 处理 UART1 环形缓冲中的所有完整帧
  │
  ├─ g_telFlag == 1 ?
  │    └─ 输出本板 BLDC 诊断数据
  │
  └─ 延时 1 ms
```

TIMER1 中断 `SpeedLoop_ISR()` 不运行复杂控制计算，只置标志：

- 每 5 ms 置 `g_spdFlag`；
- 每 4 个 5 ms 置 `g_posFlag`，当前实际位置环周期为 20 ms；
- `DIAG_EN=1` 时每 200 ms 置 `g_telFlag`。

这种架构是“中断给时 + 主循环执行”，避免在中断中运行 ADC、PID 和串口打印等重任务。

---

## 9. UART1 通信架构

### 9.1 接收链路

```text
电脑发送原始 HEX 字节
  → UART1 RX（GPIO1_3）
  → UART1_ReadItFinishCallBack()
  → 256 字节环形缓冲
  → main() 调用 UartPump()
  → 帧同步、长度检查、校验、板地址分发
  → 读/写/急停命令
```

UART1 参数为 `115200, 8 data bits, 1 stop bit, no parity, no flow control`。

### 9.2 当前有效协议

当前代码固定为 5 字节：

```text
[0xAA] [ID] [CMD] [DATA] [SUM]
```

```text
SUM = (0xAA + ID + CMD + DATA) & 0xFF
```

| CMD | 功能 | DATA |
|---:|---|---|
| `0x00` | 写入目标角度 | `0~180` |
| `0x01` | 查询当前角度 | 填充值，通常为 `0` |
| `0x02` | 本板所有电机急停 | 填充值，通常为 `0` |

步进板常用帧：

```text
AA 01 00 2D D8    运动到 45°
AA 01 00 5A 05    运动到 90°
AA 01 01 00 AC    查询当前角度
AA 00 02 00 AC    广播急停
```

查询应答为 4 字节：

```text
[0xAA] [ID] [ANGLE] [SUM]
```

### 9.3 协议处理顺序

`UartPump()` 的处理顺序是：

1. 丢弃包头前的无效字节；
2. 收集 5 字节；
3. 检查 `ID <= 4`、`CMD <= 2`；
4. 校验 SUM，检查 `DATA <= 180`；
5. `CMD=2` 时优先执行急停，不受电机 ID 归属限制；
6. 其他命令通过 `Bus_IsMyMotor()` 判断是否属于本板；
7. `CMD=1` 返回角度，`CMD=0` 下发运动目标。

> 注意：工程文档和 `main.c` 中部分旧注释仍描述 6 字节“父臂角”协议，但当前 `UartPump()` 的真实实现是上述 5 字节协议，应以可执行代码为准。

---

## 10. 步进电机控制链

### 10.1 硬件资源

| 信号 | 资源 |
|---|---|
| STEP | GPIO3_4 / APT0_PWMA |
| DIR | GPIO7_5 |
| EN | GPIO6_5，低电平使能 |
| SLEEP | GPIO4_0 |
| RESET | GPIO4_1 |
| MS1/MS2/MS3 | GPIO5_7 / GPIO5_6 / GPIO3_5 |
| REF | GPIO7_6 |
| 角度 ADC | ADC0 SOC0 / A5 / GPIO3_2 |

### 10.2 运动流程

```text
收到 AA 01 00 ANGLE SUM
  → Stepper_RunToAngle()
  → ADC_Read(g_adc0)：4 次采样取平均
  → 根据 adc0deg/adc180deg 换算当前角度 cur
  → diff = |target - cur|
  → steps = round(diff / degPerStep)
  → Stepper_Run(steps, direction)
  → 唤醒驱动、设置整步、使能、设置 DIR
  → 启动 APT0 输出 100 Hz STEP 脉冲
  → APT0_StepperCallback() 每个周期累加步数
  → 达到 g_stepTarget 后自动停止 APT0
```

当前参数：

```c
adc0deg    = 3000;   // 0° 标定 ADC
adc180deg  = 600;    // 180° 标定 ADC
degPerStep = 0.197f; // 每个 STEP 对应的机构角度
```

该控制是“运动前采样一次 + 按计算步数开环运动”，运动过程中不会持续采样 ADC 并纠偏，因此它不是实时闭环位置控制。

当前没有接入电位器时，ADC 接近 0，换算出的当前角度约为 225°，所以计算出的方向和步数不代表真实机构位置；这是未接反馈时的预期现象。

`Stepper_Stop()` 会停止 APT0、拉高 EN 并拉低 SLEEP。

---

## 11. BLDC 控制链

BLDC 使用位置环和速度环级联：

```text
串口目标角度
  → Bldc_SetTargetDeg()
  → 20 ms 位置环 BldcPos_Exec()
  → 产生目标转速 cmdRpm 和方向
  → 5 ms 速度环 BldcSpd_Run()
  → 速度 PI + 重力补偿/启动补偿
  → 生成 duty 和 dir
  → Bldc_SetOutput()
  → GPT PWM + FR + BK
```

反馈链：

- FG GPIO 边沿中断调用 `BldcFb_Isr()` 记录相邻脉冲间隔；
- `BldcFb_Update()` 根据 T 法换算 RPM 并滤波；
- ADC 通过三点标定换算关节角度；
- M3 世界角由 M2 实测角和 M3 局部角联合计算；
- 存在无 FG 转速归零、堵转解锁和急停处理。

当前 `g_boardId=2`时，`Bus_IsMyMotor(0x02/0x03/0x04)` 均为假，所以上述 BLDC 链路不对实际电机运行。

---

## 12. 中断与主循环分工

| 中断/回调 | 职责 |
|---|---|
| UART1 RX | 接收 1 字节放入环形缓冲，重新挂载下一字节接收 |
| TIMER1 / `SpeedLoop_ISR` | 只置速度环、位置环和遥测标志 |
| GPIO5_3/GPIO6_2 | 记录 BLDC FG 边沿时间戳 |
| APT0 timer | 计算已输出的步进脉冲，达到目标后停 APT |

原则是中断中尽量只记录状态或置标志，复杂计算放在 `while (1)` 中执行。

---

## 13. 当前调试形态与注意事项

1. **当前是固定步进板**

   `g_boardId=0x02`，不再从 GPIO2_7/GPIO3_0 读地址。多板部署前应恢复 `UartBus_GetAddr()` 或为不同固件明确设置板类型。

2. **控制协议和日志目前共用 UART1**

   `feature.h` 将 `DBG_PRINTF_UART_PORT` 设为 UART1，协议接收也是 UART1。单板调试时可以同时看到 `rx[5]` 和 `step...` 日志；多板 TX 并联时，多块板同时打日志可能导致总线冲突。

3. **`UART_BUS_ENABLE=0`**

   当前是单板调试模式，TX 保持 UART 功能。真正多板共线时需评估改为 `1`，并同时处理调试日志口。

4. **步进 ADC 没接入时不能验证目标角度**

   没有电位器时，可验证串口、DIR、EN 和 STEP 脉冲链路，但不能依靠 `Stepper_RunToAngle()` 验证真实角度。

5. **新命令会覆盖步进计数状态**

   `Stepper_Run()` 会重新写入 `g_stepTarget/g_stepCount/g_stepBusy`。上一次运动未结束时再发新指令，会从 0 重新计数新的步数，当前没有命令队列或忙状态拒绝机制。

6. **部分注释与实现不一致**

   - 旧注释描述 6 字节协议，当前实现为 5 字节；
   - 部分注释写 40 ms 位置环，当前 `g_posCnt >= 4` 实际为 20 ms；
   - 步进命名中的“电机1”是协议 ID01，`g_boardId=2` 中的 2 是板地址，不是电机 ID。

---

## 14. 当前步进板的最小有效路径

当 `g_boardId=0x02` 时，理解和调试当前任务只需聚焦以下路径：

```text
SystemInit()
  → UART1_Init() + ADC0_Init() + APT0_Init() + GPIO_Init()

UART_Ring_Init()
  → UART1 中断收字节

UartPump()
  → 识别 AA 01 00 ANGLE SUM
  → Bus_IsMyMotor(0x01) == true

Stepper_RunToAngle()
  → ADC0 当前角度
  → 角度差转步数

Stepper_Run()
  → GPIO 使能/方向
  → APT0 开始输出 STEP

APT0_StepperCallback()
  → 步数达标后停止
```

这条路径之外的 BLDC 位置环、速度环和 FG 反馈仍是工程的一部分，但不是当前步进板调试的关键链路。

---

## 15. 建议的后续改进

1. 清理 `main.c` 中过时的 6 字节协议和 40 ms 注释。
2. 用宏或构建配置区分“硬编码板地址”与“跳线自动识别”。
3. 为步进电机增加 ADC 有效范围和标定参数检查。
4. 增加步进忙状态查询，明确新命令是覆盖、排队还是拒绝。
5. 如果需要准确到角度，把当前“单次 ADC + 开环步数”升级为周期采样和到位纠偏。
6. 多板总线部署前，将调试日志与控制协议分离，或在发布版禁用日志。
